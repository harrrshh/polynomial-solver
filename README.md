# Polynomial Solver in C

This project reads roots of a polynomial in different bases, converts them to decimal, and calculates polynomial coefficients.

## How to Compile

```bash
gcc main.c -o solver